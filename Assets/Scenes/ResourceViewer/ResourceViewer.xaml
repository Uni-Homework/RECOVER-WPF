<Window x:Class="RECOVER.Assets.Scenes.ResourceViewer.ResourceViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Name="ThisWindow"
        Title="ResourceViewer" Height="450" Width="800">
    <Grid>
        <ListBox ItemsSource="{Binding ElementName=ThisWindow, Path=ResourcePlayers}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Image Source="{Binding ImageSource}" Width="50" Height="50" Margin="5" Grid.RowSpan="2" />

                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="5" Grid.Row="0" Grid.Column="1" />

                        <Grid Grid.Row="1" Grid.Column="1" Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Min}" VerticalAlignment="Center" />

                            <ProgressBar Grid.Column="1" Minimum="{Binding Min}" Maximum="{Binding Max}"
                                         Value="{Binding Current}" Margin="5,0" />

                            <TextBlock Text="{Binding Max}" VerticalAlignment="Center" Grid.Column="2" />
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>